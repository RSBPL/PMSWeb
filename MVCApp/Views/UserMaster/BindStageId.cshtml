@model MVCApp.Models.UserMaster

@if (ViewBag.STAGEID != null)
{
    <div>
        @Html.DropDownListFor(model => model.STAGEID, (IEnumerable<SelectListItem>)ViewBag.STAGEID, new { @class = "form-control input-sm" })
    </div>

}